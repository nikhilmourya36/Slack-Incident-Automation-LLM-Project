slack-bolt>=1.20
requests>=2.31
python-dotenv>=1.0

# Install only the SDK for the LLM provider you're using (set in .env):
# google-genai>=0.1      # LLM_PROVIDER=gemini
# anthropic>=0.34        # LLM_PROVIDER=claude
# openai>=1.40           # LLM_PROVIDER=grok (xAI is OpenAI-compatible)
